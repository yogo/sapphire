%h1 #{@project.name} > New Collection

%div{:class=>"form"}
  = form_for("collection", :url => project_collections_path(@project)) do |f|
    = render :partial => 'form', :locals => {:f => f}
  
    %p= f.submit "Create"