.block
  .content
    .inner
      %h1 Upload Data

      = render :partial => 'upload_form', :locals => {:project => @project, 
        :cancel_link => link_to("Cancel", :back, :class => "text_button_padding link_button")}

