%h1 Project: #{@project.name}

%h2 Data Collections

%a{:href => new_project_collection_path(@project)} Create a Data Collection

%ul
  - @project.data_collections.each do |c|
    %li
      %a{:href => project_collection_path(@project, c)}= c.name
      = c.schema.map{|k| k.name}.to_s
 
%a{:href => projects_path} All Projects